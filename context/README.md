# Context

Deployment-specific material: easy to find next to **`local-market.js`**.

| File | Purpose |
|------|--------|
| **`local-market.js`** | Registers skill **`local.market`** (`averageHouseholdIncome`, …). Assumptions and rationale are in the **file header comments**. Loaded after `skills/banking.js`. |
| *(optional `.md`)* | Runbooks, compliance notes — not executed by the app |

**Domain banking skills** live under **`skills/banking.js`**. This folder holds **local/geography** overrides so they’re discoverable in one place.
